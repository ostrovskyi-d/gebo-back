// routes root
const auth = require('./auth');
const post = require('./ad');
const archive = require('./archive');
const user = require('./user');

module.exports = {
    auth,
    post,
    archive,
    user,
};
